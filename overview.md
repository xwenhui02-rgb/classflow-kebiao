# ClassFlow · 课时 — 个人专属智能课程表 交付说明

## 完成了什么

根据你的《个人专属智能课程表 Apple 风格产品设计需求文档 V2.0》+ 本学期教务课表（xls），
在 `/Users/hui/WorkBuddy/网页课程表/` 落地了一个**可直接双击打开**的响应式 Web 应用（V1 Core + V1.1 Motion + V1.2 Exam + V1.3 PWA 四个阶段全部实现）。

## 交付文件

- `index.html` — 应用入口（双击即可用）
- `assets/css/` 4 个样式文件（设计令牌 / 基础 / 组件 / 视图）
- `assets/js/` 5 个脚本（数据 / 时间周次 / 状态机 / 渲染 / 主逻辑）
- `manifest.webmanifest` + `sw.js` + `icons/` — PWA 与离线缓存
- `README.md` — 使用与维护说明

## 课表数据已 100% 结构化

7 门课 20 条排课 + 4 条整周安排，跨节连堂已合并（如周一 5-8 节 14:00–17:10）、
周次规则（1-10 / 2-10 / 3-5,8 / 8-9）全部保留、单双周解析器就绪、
第 1 周社会实践与第 10/11/12 周综合实训（三位教师轮值）作为全天事件呈现。

## 关键技术决策

| 决策 | 理由 |
|------|------|
| 零构建、原生 JS | 双击即用，可直接部署到 GitHub Pages；需求文档 V1 也建议"轻量版可先用 HTML/CSS/JS" |
| 第 1 周起始日 = 2026-09-07 | 2026 年 9 月第一个周一；**可在设置中一键修改**，全部周次计算随之联动 |
| 时间旅行（预览模式） | 自创增强：现在开学前，拨到 9月9日 16:00 就能完整看到"正在上课"状态，不会让首屏显得"空" |
| localStorage 存个人数据 | 教务没给考试日程，考试需手动添加；数据可导出 JSON 备份 |

## 验证结果（真实浏览器实测）

- 390px 手机 / 1440px 桌面：无横向溢出、无控制台错误
- 浅色 / 深色主题均通过对比度与可读性检查
- 教学周计算、节假日停课、整周实训、课程进度、冲突检测：引擎单元测试全过
- `.ics` 导出：102 个日历事件（含上课前提醒），可导入系统日历

## 待办 / 你可以继续做的事

1. 在设置里确认「第 1 周起始日」是否为 9月7日（若学校实际 9月1日 前后开学，改这里即可）。
2. 中秋/国庆按停课处理是默认假设，若学院有调课通知，用课程详情里的"标记本次调课"修正。
3. 考试日程出来后在「考试」页添加，首页会自动出现倒计时与红点提醒。
4. 想要手机随时访问，可以让我把它发布成在线链接（或推到 GitHub Pages）。
